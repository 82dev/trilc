program ]> stmt* EOF

stmt ]> block
     |  var
     |  reAss
     |  ';'

var ]> ID ':' (INT | BOOL) ('=' expr)? ';'

reAss ]> ID '=' expr ';'

block ]> '{'
            stmt*
         '}'

expr ]> term

term ]> factor ('+'|'-' factor)*
     |  factor

factor ]> unary ('*'|'/' unary)*
       |  primary

unary ]> ('!'|'-') unary
      |  primary

primary ]> NUM
        |  TRUE
        |  FALSE
        |  STRING