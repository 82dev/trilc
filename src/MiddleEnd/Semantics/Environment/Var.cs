namespace trilc
{
    struct Var
    {
        public string name;
        public string type;

        public Var(string name, string type)
        {
            this.name = name;
            this.type = type;
        }
    }
}